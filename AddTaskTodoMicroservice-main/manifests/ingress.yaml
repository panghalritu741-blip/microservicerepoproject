apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: add-task-ingress
    
spec:
  ingressClassName: nginx
  ingressClassName: nginx
  rules:
  - host: add.6weathers.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: add-task
            port: 
              number: 80
